[build]
  builder = "NIXPACKS"
  buildCommand = "npm run build"

[env]
  DB_PATH = "/data/stablecoins.db"

[deploy]
  startCommand = "npm start"
  healthcheckPath = "/health"
  restartPolicyType = "on_failure"
